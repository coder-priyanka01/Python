name = "priyanka" 

print(len(name)) # length of the string

print(name.endswith("nka")) # check if the string ends with "nka"
print(name.startswith("pr")) # check if the string starts with "pr"

print(name.capitalize()) # capitalize the first letter of the string

b = "priyanka is a best one"
replaced_string = b.replace("best", "good") # replace "best" with "good"
print(replaced_string) 
