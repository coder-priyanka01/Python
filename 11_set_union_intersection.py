s1 = {1,45,6}
s2 = {1,5,6,7,8,9}

print(s1.union(s2))  # union of two sets, returns a new set with all elements from both sets
print(s1.intersection(s2))  # intersection of two sets, returns a new set with elements common to both sets
